{{- include "chart.validateDbConfig" . -}}
{{- $dbTls := .Values.db.tls | default dict -}}
{{- $dbTlsCaCert := $dbTls.caCert | default "" -}}
{{- $dbTlsMode := $dbTls.mode | default "none" -}}
{{- if .Values.redirector.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.resourceNamePrefix" . }}redirector-config
type: Opaque
stringData:
  redirector.yml: |-
    db:
      host: {{ default (printf "%s-postgresql.%s" .Release.Name .Release.Namespace) .Values.db.host }}
      port: {{ .Values.db.port }}
      dbname: {{ .Values.db.dbname }}
      user: {{ .Values.db.user }}
      password: {{ .Values.db.password }}
      ssl_mode: {{ .Values.db.sslMode | lower | title }}
{{- if ne $dbTlsMode "none" }}
      tls:
        mode: {{ $dbTlsMode }}
{{- if $dbTlsCaCert }}
        ca_cert: {{ $dbTlsCaCert | toJson }}
{{- end }}
{{- end }}
    log:
      format: {{ .Values.log.format }}
    server:
      addr: {{ .Values.redirector.addr }}
      base_redirect_url: {{ .Values.redirector.baseRedirectUrl }}
{{- end }}
